#ifndef UTILS_H
#define UTILS_H

#include <string>
#include <utility>

// Function to load configuration from the config.json file
std::pair<std::string, std::string> load_config();

#endif // UTILS_H